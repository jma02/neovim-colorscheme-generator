export interface DroppedItem {
    fillColor: string
}

export interface ThemeFile {
    accent: string,
    bg: string,
    fg: string,
    ui: string,
    string: string, // lol
    func: string,
    operator: string,
    comment: string,
    error: string,
}
